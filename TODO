桌面组件

自习计时

数据查看

我需要如下的效果：

每个项目都没有边框
在停止时，项目按顺序排序，时间用灰色，正常字体大小，介绍折叠
在运行/暂停时：
  pending按顺序显示在running的下方，同停止时样式
  running显示在top: 30%(若100vh > 400px)或20px的位置(动画)，时间用primary, 字体大小h1, 标题h2(动画过渡)，介绍展开（动画）
  done按顺序显示在running的上方（位置动画），同停止时样式