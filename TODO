添加comp: ScheduledNotice extends Notice 于 @comp

配置:
  begin: string // "hh:mm"
  end: string // "hh:mm"
  debugShow: bool

配置界面:
  begin: input[time]
  end: input[time]
  debugShow: checkbox

显示:
  当前当地时间在 begin, end 范围内 或 debugShow，显示：
    正常内容，但所有文字居中
  否则：
    完全不显示（包括背景）

注意事项:
  参见 @Notice.tsx 的实现
